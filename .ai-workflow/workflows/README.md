# Workflows

- `main.workflow.yaml`：阶段顺序与审核门禁。
- `document-reference-map.yaml`：文档引用关系与引用原则。
- `template-config.yaml`：阶段到模板映射。

执行要求：
- 严格按阶段推进，未“通过”不得进入下一阶段。
- 必须引用项缺失不得提审。
- 模板缺失或映射错误不得提审。
- HTML 原型必须一屏一审。
