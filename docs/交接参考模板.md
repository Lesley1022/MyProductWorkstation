# 交接信息参考模板（交接给设计师时使用）

## 项目信息
- **项目标识称**：{{项目标识}}（英文标识：{{项目标识}}）
- **版本号**：{{版本号}}（如 v2.1.0）
- **平台标识**：{{平台标识}}（`asset-management` / `oam-collaboration`）

## 输入材料路径
- **BRD**：`docs/{{平台标识}}/{{项目标识}}/{{版本号}}/requirements/BRD_{{YYYYMMDD}}.md`
- **MRD**：`docs/{{平台标识}}/{{项目标识}}/{{版本号}}/requirements/MRD_{{YYYYMMDD}}.md`
- **研究员报告目录**：`docs/{{平台标识}}/{{项目标识}}/{{版本号}}/research/`（包含竞品分析、用户反馈、政策调研、综合摘要等）
- **架构图**：`docs/{{平台标识}}/{{项目标识}}/{{版本号}}/architecture/架构图_{{YYYYMMDD}}.md`

## 核心功能列表
- {{功能点1}}
- {{功能点2}}
- {{功能点3}}
- ……

## 范围边界
- **本期做（scope_in）**：
  - {{功能模块A}}
  - {{功能模块B}}
- **本期不做（scope_out）**：
  - {{明确排除的内容}}
  - {{明确排除的内容}}

## 核心页面清单
1. **{{页面名称1}}**  
   - **主要区域**：查询区（字段：{{字段A}}、{{字段B}}）、列表区（展示字段：{{X}}、{{Y}}、{{Z}}）、操作按钮（新增、导出、详情）。
   - **参考截图**：[附件或链接]（可选）

2. **{{页面名称2}}**  
   - **主要区域**：选项卡（{{选项卡A}}、{{选项卡B}}）、表单区（字段：{{字段C}}、{{字段D}}）、提交按钮。
   - **参考截图**：[附件或链接]（可选）

3. **{{页面名称3}}**  
   - **主要区域**：列表区、详情弹窗、操作按钮（编辑、停用）。
   - **参考截图**：[附件或链接]（可选）

## 核心交互需求
- 点击 **“{{按钮名称}}”**：跳转至 {{目标页面}} / 弹出 {{弹窗类型}} / 执行 {{具体行为}}。
- **选项卡切换**：页面需包含 {{选项卡名称}}，点击时切换对应内容区域，动画效果可选。
- **表单提交**：点击“保存”后，校验必填项（{{字段列表}}），若成功则弹出“保存成功”提示，若失败则提示具体错误。
- **列表操作**：每行数据包含“编辑”、“删除”按钮，删除前需弹窗二次确认。

## 主流程与异常流程
- **主流程**：用户从 {{起始页面/操作}} 到 {{终点页面/状态}} 的完整路径。
  - 步骤1：{{描述}}
  - 步骤2：{{描述}}
  - 步骤3：{{描述}}
- **异常流程1**：当 {{条件（如网络超时、数据不存在）}} 发生时，系统应 {{处理方式（如提示错误、回退）}}。
- **异常流程2**：当 {{条件}} 发生时，系统应 {{处理方式}}。

## 验收标准
- **页面加载时间**：不超过 2 秒。
- **交互响应**：所有按钮点击均有明确反馈（视觉变化或提示）。
- **列表功能**：支持按指定字段排序，分页正常。
- **表单校验**：必填项未填时，对应字段边框标红并显示提示。
- **跨浏览器**：在 Chrome、Edge 最新版本中显示正常。

## 设计参考（附截图或链接）
- **竞品参考**：
  - 竞品A 的 {{具体页面/功能}} 布局：[链接或截图]
  - 竞品B 的 {{具体样式/配色}}：[链接或截图]
- **通用规范**：请严格遵循 `docs/通用设计规范.md` 中关于间距、颜色、字体、交互的规定。
- **HTML 原型规范**：请调用 `html-prototyping` Skill，确保生成的 HTML 符合现代布局和交互要求。

## 输出要求
- **第一阶段（HTML 原型）**：
  - 文件路径：`docs/{{平台标识}}/{{项目标识}}/{{版本号}}/design/原型_{{YYYYMMDD}}.html`
  - 设计说明：`设计说明_{{YYYYMMDD}}.md`（需包含设计思路、遵循的规范、交互细节、参考来源）
- **审核流程**：HTML 原型完成后提交审核，审核通过后主动询问是否需要生成 Figma 原型。
- **若需 Figma 原型**：生成 Figma 链接记录文件 `Figma链接_{{{YYYYMMDD}}.md`，并再次提交审核。

---
> **使用说明**：请将 `{{}}` 中的占位符替换为实际项目信息，并根据需要增删内容。此模板可与 `pm.agent.md` 中的交接消息结合使用，确保设计师获得足够详细的指引。